import { defineConfig } from "cypress";

export default defineConfig({
  e2e: {
    // Just remove or comment this section to skip custom logic
    // setupNodeEvents: (on, config) => {
    //   // Custom plugin logic here...
    // },
  },
});
